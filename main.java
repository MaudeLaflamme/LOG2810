import java.util.*; // importe toute les librairies java

public class main 
{
	/**
	 * Fonction principale
	 * @param args (non utilise)
	 */
	public static void main(String[] args) {
		
		Interface inter = new Interface();	
		Graphe graph = new Graphe();
		inter.afficherInterface(graph);

		
    }
}